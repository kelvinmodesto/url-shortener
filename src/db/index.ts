import { ContextStrategy } from './base';
import { MongoDBStrategy } from './mongodb';

const main = {
  ContextStrategy,
  MongoDBStrategy
};

export { main, ContextStrategy, MongoDBStrategy };
export default main;
